---
---
<section class="relative bg-white pt-32 pb-20 overflow-hidden">
  <!-- Background Pattern (Modern Subtle Grid) -->
  <div class="absolute inset-0 bg-[linear-gradient(to_right,#8080800a_1px,transparent_1px),linear-gradient(to_bottom,#8080800a_1px,transparent_1px)] bg-[size:24px_24px]"></div>
  <div class="absolute left-0 right-0 top-0 -z-10 m-auto h-[310px] w-[310px] rounded-full bg-primary opacity-20 blur-[100px]"></div>

  <div class="container mx-auto px-6 relative z-10 text-center">
    
    <!-- Trust Badge -->
    <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-blue-50 border border-blue-100 text-primary text-xs font-semibold tracking-wide uppercase mb-8">
      <span class="relative flex h-2 w-2">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-primary"></span>
      </span>
      Specialized for Contractors & Service Business
    </div>

    <!-- Headline (Clean & Heavy) -->
    <h1 class="text-5xl md:text-7xl font-bold text-dark tracking-tight leading-tight mb-6 max-w-4xl mx-auto">
      Web Anda <span class="bg-gradient-to-r from-primary to-blue-600 bg-clip-text text-transparent">Lambat?</span> <br>
      Anda Sedang Membakar Uang.
    </h1>

    <!-- Subheadline (Readable) -->
    <p class="text-xl text-gray-500 max-w-2xl mx-auto mb-10 leading-relaxed">
      Kami merekayasa Landing Page dengan standar <strong>High-Performance</strong>. 
      Garansi load time <2 detik, kalkulator interaktif, dan tracking pixel akurat. 
      Bukan sekadar desain cantik.
    </p>

    <!-- CTAs (Professional Buttons) -->
    <div class="flex flex-col sm:flex-row justify-center items-center gap-4">
      <a 
        href="#calculator" 
        class="group relative px-8 py-4 bg-primary text-white font-bold rounded-xl shadow-lg hover:shadow-primary/30 hover:-translate-y-0.5 transition-all overflow-hidden"
      >
        <span class="relative z-10">Lihat Demo Kalkulator</span>
        <div class="absolute inset-0 h-full w-full scale-0 rounded-xl transition-all duration-300 group-hover:scale-100 group-hover:bg-blue-700/50"></div>
      </a>
      
      <a 
        href="#" 
        class="px-8 py-4 bg-white text-dark font-semibold border border-gray-200 rounded-xl hover:border-gray-300 hover:bg-gray-50 transition flex items-center gap-2"
      >
        <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path></svg>
        Audit Web Gratis
      </a>
    </div>

    <!-- Social Proof / Metrics (Minimalist) -->
    <div class="mt-16 grid grid-cols-2 md:grid-cols-4 gap-8 max-w-4xl mx-auto border-t border-gray-100 pt-8">
      <div>
        <h4 class="text-3xl font-bold text-dark">0.4s</h4>
        <p class="text-sm text-gray-400 font-medium mt-1">Avg. Load Time</p>
      </div>
      <div>
        <h4 class="text-3xl font-bold text-dark">100</h4>
        <p class="text-sm text-gray-400 font-medium mt-1">Lighthouse Score</p>
      </div>
      <div>
        <h4 class="text-3xl font-bold text-dark">3.5x</h4>
        <p class="text-sm text-gray-400 font-medium mt-1">Conversion Uplift</p>
      </div>
      <div>
        <h4 class="text-3xl font-bold text-dark">A+</h4>
        <p class="text-sm text-gray-400 font-medium mt-1">SEO Structure</p>
      </div>
    </div>

  </div>
</section>
