---
---
<div 
  class="fixed bottom-6 left-6 z-40 hidden md:flex flex-col gap-2"
  x-data="{ show: true }"
  x-show="show"
  x-transition:enter="transition ease-out duration-300"
  x-transition:enter-start="opacity-0 translate-y-4"
  x-transition:enter-end="opacity-100 translate-y-0"
>
  <div class="bg-dark/90 backdrop-blur-md border border-gray-700 p-4 rounded-xl shadow-2xl text-white w-64">
    <div class="flex justify-between items-center mb-3">
      <h4 class="text-xs font-bold uppercase tracking-widest text-gray-400 flex items-center gap-2">
        <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
        System Status
      </h4>
      <button @click="show = false" class="text-gray-500 hover:text-white">
        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
      </button>
    </div>
    
    <div class="space-y-3">
      <!-- Metric 1 -->
      <div class="flex justify-between items-center text-sm">
        <span class="text-gray-300">Lighthouse Score</span>
        <span class="font-bold text-green-400">100/100</span>
      </div>
      <div class="w-full bg-gray-700 h-1 rounded-full overflow-hidden">
        <div class="bg-green-500 h-full w-full"></div>
      </div>

      <!-- Metric 2 -->
      <div class="flex justify-between items-center text-sm">
        <span class="text-gray-300">Page Load</span>
        <span class="font-bold text-blue-400">0.4s</span>
      </div>

      <!-- Metric 3 -->
      <div class="flex justify-between items-center text-sm">
        <span class="text-gray-300">JS Bundle</span>
        <span class="font-bold text-yellow-400">18 kB</span>
      </div>
    </div>

    <div class="mt-4 pt-3 border-t border-gray-700 text-[10px] text-gray-500 text-center">
      Powered by High-Performance Engine
    </div>
  </div>
</div>
